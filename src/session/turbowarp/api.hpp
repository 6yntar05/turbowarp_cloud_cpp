#pragma once

#include <boost/json.hpp>
#include <string>

namespace turbowarp {
    class api {
        public:
            static std::string analize_data(std::string data);
        private:
    };
} // namespace turbowarp_api